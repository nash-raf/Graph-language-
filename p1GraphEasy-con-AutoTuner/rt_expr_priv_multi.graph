graph G {
  directed: true;
  edges: file "rtest_small.txt";
};

int n = numVertices(G);
int a = 0;
int b = 0;

for each vertex u in G {
  for each neighbor v of u in G {
    a = a + v;
    b = b + 1;
  }
}

print a;
print b;
