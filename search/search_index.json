{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"GraphEasy Language","text":"<p>GraphEasy is an efficient, easy-to-use DSL for describing graphs and writing graph algorithms. Programs are written in <code>.graph</code> files, compiled by <code>GraphProgram</code>, and linked with a C++ runtime.</p> <ul> <li>Repo: https://github.com/nash-raf/Graph-language-</li> <li>Getting Started: See Getting Started</li> </ul>"},{"location":"#what-you-can-do","title":"What you can do","text":"<ul> <li>Load graphs from an edgelist file or define them inline</li> <li>Run built-in graph queries (BFS, DFS, Dijkstra, and more)</li> <li>Write custom algorithms with <code>for each</code> loops over vertices, edges, neighbors, or sets</li> <li>Use arrays, sets, conditionals, and while-loops in a C-like syntax</li> <li>Filter and combine graphs with graph comprehensions</li> </ul>"},{"location":"#documentation","title":"Documentation","text":"Section Description Getting Started How to write and run a small GraphEasy program Grammar Core syntax from <code>Base.g4</code> and benchmark extensions Algorithms The five benchmark algorithms in GraphEasy Results Placeholder for plots and benchmark summaries"},{"location":"#quick-example","title":"Quick example","text":"<pre><code>graph G {\n  edges: file 'edgelist.txt';\n};\n\nquery path : \"bfs\" of G;\nprint path;\n</code></pre>"},{"location":"#running-a-program-p2grapheasy","title":"Running a program (p2GraphEasy)","text":"<p>From the <code>p2GraphEasy</code> directory:</p> <pre><code>./1run.sh test.graph test.graph   # compile GraphProgram + your program\n./2final_run.sh                   # link and run\n</code></pre> <p>See Grammar for syntax and Algorithms for complete benchmark programs.</p>"},{"location":"algorithms/","title":"Algorithms","text":"<p>This page lists the five aligned algorithms used in the benchmark suite.</p>"},{"location":"algorithms/#bfs-parent","title":"BFS Parent","text":"<p>BFS parent computes a breadth-first traversal tree from source vertex <code>0</code>. Each discovered vertex stores the vertex that first reached it.</p> <pre><code>graph G {\n  edges: file \"graph.txt\";\n};\n\nint n = numVertices(G);\nint visited[n];\nint parent[n];\nint frontier[n];\nint next_frontier[n];\nint frontier_size = 1;\nint next_size = 0;\nint init_i = 0;\n\nwhile (init_i &lt; n) {\n  visited[init_i] = 0;\n  parent[init_i] = -1;\n  init_i = init_i + 1;\n}\n\nfrontier[0] = 0;\nvisited[0] = 1;\nparent[0] = 0;\n\nwhile (frontier_size &gt; 0) {\n  next_size = 0;\n  int i = 0;\n\n  while (i &lt; frontier_size) {\n    int v = frontier[i];\n    for each neighbor u of v in G {\n      if (visited[u] == 0) {\n        visited[u] = 1;\n        parent[u] = v;\n        next_frontier[next_size] = u;\n        next_size = next_size + 1;\n      }\n    }\n    i = i + 1;\n  }\n\n  swap(frontier, next_frontier);\n  frontier_size = next_size;\n}\n</code></pre>"},{"location":"algorithms/#connected-components","title":"Connected Components","text":"<p>Connected components uses label propagation. Each vertex starts with its own id, then lower labels spread through the graph until convergence.</p> <pre><code>graph G {\n  edges: file \"graph.txt\";\n};\n\nint n = numVertices(G);\nint id[n];\nset frontier;\nset next;\n\nfor each vertex v in G {\n  id[v] = v;\n}\n\nfrontier = G.nodes;\n\nwhile (setSize(frontier) &gt; 0) {\n  next = {};\n  for each element u in frontier {\n    for each neighbor v of u in G {\n      if (id[u] &lt; id[v]) {\n        id[v] = id[u];\n        next.add(v);\n      }\n    }\n  }\n  frontier = next;\n}\n</code></pre>"},{"location":"algorithms/#kcore","title":"KCore","text":"<p>KCore removes vertices whose degree is below <code>k</code>. Removing a vertex reduces the degree of its live neighbors, which may create more removals.</p> <pre><code>graph G {\n  edges: file \"graph.txt\";\n};\n\nint n = numVertices(G);\nint k = 5;\nint deg[n];\nint alive[n];\nset frontier;\nset next;\n\nfor each vertex v in G {\n  deg[v] = 0;\n  alive[v] = 1;\n}\n\nfor each vertex u in G {\n  for each neighbor v of u in G {\n    deg[u] = deg[u] + 1;\n  }\n}\n\nfrontier = {};\nfor each vertex v in G {\n  if (deg[v] &lt; k) {\n    frontier.add(v);\n  }\n}\n\nwhile (setSize(frontier) &gt; 0) {\n  next = {};\n  for each element u in frontier {\n    if (alive[u] == 1) {\n      alive[u] = 0;\n      for each neighbor v of u in G {\n        if (alive[v] == 1) {\n          deg[v] = deg[v] - 1;\n          if (deg[v] &lt; k) {\n            next.add(v);\n          }\n        }\n      }\n    }\n  }\n  frontier = next;\n}\n</code></pre>"},{"location":"algorithms/#pagerank","title":"PageRank","text":"<p>PageRank runs fixed-count power iteration. The benchmark uses damping factor <code>0.85</code> and <code>20</code> iterations.</p> <pre><code>graph G {\n  edges: file \"graph.txt\";\n};\n\nint n = numVertices(G);\nreal damp = 0.85;\nreal one_over_n = 1.0 / n;\nreal beta = (1.0 - damp) / n;\nreal cur_rank[n];\nreal next_rank[n];\nint out_degree[n];\nint round = 0;\nint max_rounds = 20;\n\nfor each vertex v in G {\n  cur_rank[v] = one_over_n;\n  next_rank[v] = beta;\n  out_degree[v] = 0;\n}\n\nfor each vertex u in G {\n  for each neighbor v of u in G {\n    out_degree[u] = out_degree[u] + 1;\n  }\n}\n\nwhile (round &lt; max_rounds) {\n  round = round + 1;\n\n  for each vertex v in G {\n    next_rank[v] = beta;\n  }\n\n  for each vertex u in G {\n    int outd = out_degree[u];\n    if (outd &gt; 0) {\n      real contrib = damp * cur_rank[u] / outd;\n      for each neighbor v of u in G {\n        next_rank[v] = next_rank[v] + contrib;\n      }\n    }\n  }\n\n  for each vertex v in G {\n    cur_rank[v] = next_rank[v];\n  }\n}\n</code></pre>"},{"location":"algorithms/#sssp","title":"SSSP","text":"<p>SSSP uses a frontier-based Bellman-Ford style relaxation over a weighted graph.</p> <pre><code>graph G {\n  edges: file \"weighted_graph.txt\";\n  TRUE\n};\n\nint n = numVertices(G);\nint inf = 2147483647;\nint dist[n];\nset frontier;\nset next;\nint src = 0;\nint rounds = 0;\n\nfor each vertex v in G {\n  dist[v] = inf;\n}\n\ndist[src] = 0;\nfrontier = {0};\n\nwhile (setSize(frontier) &gt; 0) {\n  if (rounds == n) {\n    print \"negative cycle\";\n    break;\n  }\n\n  next = {};\n  for each element u in frontier {\n    if (dist[u] != inf) {\n      for each neighbor v of u in G {\n        int nd = dist[u] + weight(G, u, v);\n        if (nd &lt; dist[v]) {\n          dist[v] = nd;\n          next.add(v);\n        }\n      }\n    }\n  }\n  frontier = next;\n  rounds = rounds + 1;\n}\n</code></pre>"},{"location":"grammar/","title":"Grammar","text":"<p>This page summarizes the language syntax used by GraphEasy programs. The first part follows the core grammar in <code>Base.g4</code>; the second part documents extensions used by the benchmark programs.</p>"},{"location":"grammar/#program-structure","title":"Program Structure","text":"<p>A program is a sequence of statements and function definitions:</p> <pre><code>program: (statement | function)* EOF;\n</code></pre> <p>Statements include graph definitions, variable declarations, conditionals, loops, graph comprehensions, queries, graph display, graph mutation, function calls, and print statements.</p>"},{"location":"grammar/#graph-definitions","title":"Graph Definitions","text":"<p>Graphs are declared with the <code>graph</code> keyword:</p> <pre><code>graph G {\n  nodes: 0, 1, 2, 3;\n  edges: 0-&gt;1, 1-&gt;2, 2-&gt;3;\n};\n</code></pre> <p>Graphs can also load edges from a file:</p> <pre><code>graph G {\n  edges: file \"edgelist.txt\";\n};\n</code></pre> <p>Adjacency-list input is represented with <code>fileAdj</code>:</p> <pre><code>graph G {\n  edges: fileAdj \"adjacency.txt\";\n};\n</code></pre> <p>Weighted input is used by the benchmark runtime with <code>TRUE</code> inside the graph block:</p> <pre><code>graph G {\n  edges: file \"weighted_edgelist.txt\";\n  TRUE\n};\n</code></pre>"},{"location":"grammar/#types-and-variables","title":"Types And Variables","text":"<p>Core scalar types are:</p> Type Meaning <code>int</code> Integer <code>real</code> Floating-point value <code>bool</code> Boolean <code>string</code> String <code>vertex</code> Vertex value <code>edge</code> Edge value <code>graph</code> Graph value <p>Examples:</p> <pre><code>int source = 0;\nreal damping = 0.85;\nbool active = true;\nstring label = \"bfs\";\n</code></pre>"},{"location":"grammar/#expressions","title":"Expressions","text":"<p>The core expression grammar supports arithmetic, function calls, identifiers, integers, reals, booleans, and parentheses:</p> <pre><code>int x = 2 + 3 * 4;\nreal y = (1.0 - damping) / n;\nint z = numVertices(G);\n</code></pre> <p>Comparison and logical operators are used in conditions:</p> <pre><code>if (dist[v] &gt; dist[u] + w) {\n  dist[v] = dist[u] + w;\n}\n\nif (a &lt; b &amp;&amp; b &lt; c) {\n  print \"ordered\";\n}\n</code></pre>"},{"location":"grammar/#loops","title":"Loops","text":"<p>GraphEasy has <code>while</code> loops:</p> <pre><code>while (round &lt; max_rounds) {\n  round = round + 1;\n}\n</code></pre> <p>It also has graph traversal loops:</p> <pre><code>for each vertex v in G {\n  print v;\n}\n\nfor each edge u, v in G {\n  print u;\n}\n\nfor each neighbor v of u in G {\n  print v;\n}\n</code></pre>"},{"location":"grammar/#graph-queries","title":"Graph Queries","text":"<p>Built-in graph queries use this form:</p> <pre><code>query result : \"bfs\" of G;\nprint result;\n</code></pre> <p>The grammar accepts a string query name. Runtime support determines which query strings are implemented.</p>"},{"location":"grammar/#graph-comprehension","title":"Graph Comprehension","text":"<p>Graph comprehensions filter an existing graph:</p> <pre><code>H = [G where degree &gt; 2];\nK = [G where degree &gt; 2 &amp;&amp; connected with 0];\n</code></pre> <p>Supported graph conditions include degree comparisons, connectivity with a vertex, logical <code>&amp;&amp;</code>, logical <code>||</code>, and parentheses.</p>"},{"location":"grammar/#graph-mutation","title":"Graph Mutation","text":"<p>Nodes and edges can be added to or removed from a graph:</p> <pre><code>add 4 to G;\nadd 2-&gt;4 to G;\nremove 0-&gt;1 from G;\n</code></pre>"},{"location":"grammar/#functions","title":"Functions","text":"<p>Functions have a return type, name, parameter list, and block:</p> <pre><code>fn int add_one(int x) {\n  return x + 1;\n}\n\nint y = add_one(4);\n</code></pre>"},{"location":"grammar/#print-and-show","title":"Print And Show","text":"<pre><code>print \"done\";\nprint x;\nprint edges of G;\nprint nodes of G;\nprint graph of G;\nshow G;\n</code></pre>"},{"location":"grammar/#benchmark-extensions","title":"Benchmark Extensions","text":"<p>The benchmark algorithms use implementation features beyond the minimal <code>Base.g4</code> listing.</p>"},{"location":"grammar/#arrays","title":"Arrays","text":"<pre><code>int n = numVertices(G);\nint parent[n];\nreal rank[n];\n\nparent[0] = 0;\nrank[0] = 1.0 / n;\n</code></pre>"},{"location":"grammar/#sets","title":"Sets","text":"<pre><code>set frontier;\nset next;\n\nfrontier = {0};\nnext = {};\nnext.add(v);\n\nwhile (setSize(frontier) &gt; 0) {\n  frontier = next;\n}\n</code></pre>"},{"location":"grammar/#set-traversal","title":"Set Traversal","text":"<pre><code>for each element u in frontier {\n  print u;\n}\n</code></pre>"},{"location":"grammar/#runtime-helpers","title":"Runtime Helpers","text":"<p>Common helper calls in benchmark programs:</p> Helper Use <code>numVertices(G)</code> Number of vertices <code>weight(G, u, v)</code> Edge weight for weighted SSSP <code>setSize(frontier)</code> Number of elements in a set <code>swap(a, b)</code> Swap array-backed frontier buffers"},{"location":"results/","title":"Results","text":"<p>This page summarizes the current aligned benchmark logs.</p>"},{"location":"results/#violin-plot","title":"Violin Plot","text":"<p>The normal violin plot shows runtime distributions across all aligned systems and algorithms.</p> <p></p> <p>Individual algorithm plots:</p> Algorithm Plot BFS parent BFS violin Connected components CC violin KCore KCore violin PageRank PageRank violin SSSP SSSP violin"},{"location":"results/#heatmap","title":"Heatmap","text":"<p>The heatmap compares GraphIt, Ligra, and Graptor against <code>p2GraphEasy</code> across four synthetic graph seeds. Rows are seeds and columns are algorithms.</p> <p>Each cell is:</p> <pre><code>p2GraphEasy runtime / system runtime\n</code></pre> <p>So <code>1.00</code> means equal runtime to <code>p2GraphEasy</code>; values above <code>1.00</code> mean the compared system ran faster than <code>p2GraphEasy</code>.</p> <p></p> <p>Exact values are available as CSV.</p>"},{"location":"results/#benchmark-scope","title":"Benchmark Scope","text":"Item Description Baseline <code>p2GraphEasy</code> Compared systems GraphIt, Ligra, Graptor Seeds shown 1, 2, 3, 4 Algorithms BFS parent, connected components, KCore, PageRank, and SSSP Graph routing BFS parent, CC, KCore, and PageRank use unweighted synthetic violin graphs; SSSP uses weighted synthetic violin graphs <p>The benchmark suite keeps unweighted algorithms on unweighted synthetic violin graphs and SSSP on weighted synthetic violin graphs.</p>"},{"location":"tutorial/","title":"Getting Started","text":"<p>This page shows the smallest path from a <code>.graph</code> file to a running GraphEasy program.</p>"},{"location":"tutorial/#minimal-program","title":"Minimal Program","text":"<pre><code>graph G {\n  edges: file \"edgelist.txt\";\n};\n\nquery path : \"bfs\" of G;\nprint path;\n</code></pre> <p>The graph file is a plain edgelist. For an unweighted graph, each line contains two integer vertex ids:</p> <pre><code>0 1\n1 2\n1 3\n2 4\n</code></pre> <p>For a weighted graph, each line contains <code>source destination weight</code>:</p> <pre><code>0 1 7\n1 2 3\n2 4 5\n</code></pre>"},{"location":"tutorial/#run-a-program","title":"Run A Program","text":"<p>From a GraphEasy implementation directory such as <code>p2GraphEasy</code>:</p> <pre><code>./1run.sh test.graph test.graph\n./2final_run.sh\n</code></pre> <p>The first command compiles the language frontend and the <code>.graph</code> program. The second command links the generated objects with the runtime and executes the final binary.</p>"},{"location":"tutorial/#project-layout","title":"Project Layout","text":"<p>The repository has multiple implementation families:</p> Directory Purpose <code>p1GraphEasy</code> Baseline GraphEasy implementation <code>p2GraphEasy</code> Optimized GraphEasy implementation <code>p1GraphEasy-con-AutoTuner</code> P1 with autotuner support <code>p2GraphEasy-con-AutoTuner</code> P2 with autotuner support <code>parallel</code> Parallel experiments and workloads"},{"location":"tutorial/#documentation-sections","title":"Documentation Sections","text":"<p>Use the rest of the documentation in this order:</p> Section What it covers Grammar Core syntax from <code>Base.g4</code> and benchmark language extensions Algorithms BFS parent, connected components, KCore, PageRank, and SSSP in GraphEasy Results Placeholder for benchmark plots, tables, and notes"}]}