fn int main() {
  int x = 1000000;
  int y = 3;


  // Outer loop depends on x, inner loop increments y until 30.
  while (x > 0) {
    x = x - 1;
    print x;
  }

  // return something that depends on the loops so the function's behaviour is visible
  return x + y;
}
